{
  "name": "blog.kanzan.io",
  "version": "0.0.1",
  "description": "The Kanzan Blog",
  "author": "Weave",
  "scripts": {
    "build": "jekyll build",
    "deploy": "git subtree push --prefix www origin gh-pages"
  }
}
